language:
    - C
    - C++

sudo: false

git:
    depth: 1

os:
    - linux
    - windows
    - osx

addons:
    apt:
        updated:true
        packages:
            - libcunit1
            - libcunit1-dev
    homebrew:
        update: true # one needs to update brew.
        packages:
            - cunit
#    chocolatey:
#        packages:
#            # - libcunit
        

script:
    - cmake . 
    - cmake --build . -- 
    - ./bin/unit-test
