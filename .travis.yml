language:
    - C
    - C++

sudo: false

git:
    depth: 1

os:
    - linux
    - windows
    - osx

addons:
    apt:
        packages:
            - libcunit1
            - libcunit1-dev
    brew:
        packages:
            - cunit
#    chocolatey:
#        packages:
#            # - libcunit
        

script:
    - cmake . -G "Unix Makefiles"
    - cmake --build . -- 
